# Gowatch configuration file
# Modifiers go first, followed by the hotkey, seperated by a '+'
# Supported modifiers:
# CMD, ALT, WIN, SUPER, CTRL, OPTION, SHIFT
# Supported keys:
# 0-1, A-Z
hotkeys:
  startpause: "Alt+W"
  reset: "Alt+R"
